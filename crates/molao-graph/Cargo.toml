[package]
name = "molao-graph"
description = "The Molao citation graph: authority scoring, neighbourhood queries, and a byte-stable graph root."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
molao-core.workspace = true
molao-corpus.workspace = true
blake3.workspace = true
# Attestations are self-authenticating: the signature check in `treatment` is
# what stands between a forged "overruled" and a practitioner, because the
# treatments table is excluded from the release root and nothing else covers it.
ed25519-dalek.workspace = true
hex.workspace = true
rusqlite.workspace = true
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
tracing.workspace = true
